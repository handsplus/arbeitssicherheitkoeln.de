export default function Loading() {
  return (
    <div
      style={{
        padding: "2rem",
        textAlign: "center",
        color: "#1f3931",
        fontFamily: "system-ui, sans-serif",
      }}
    >
      Seite wird geladen…
    </div>
  );
}
